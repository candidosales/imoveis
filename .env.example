GOOGLE_MAPS_API_KEY=
# Same key as above, exposed to the browser for the Maps JavaScript API (client
# map view). Google Maps JS API keys are meant to be public — restrict by
# HTTP referrer in Google Cloud Console instead of keeping this secret.
VITE_GOOGLE_MAPS_API_KEY=
DATABASE_PATH=./data/caucaia-imoveis.sqlite
